{
  "name": "ai-popper",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "chat": "yarn && yarn workspace shared-lib build && yarn workspace cli chat",
    "search": "yarn && yarn workspace shared-lib build && yarn workspace cli search",
    "server": "yarn && concurrently \"yarn workspace shared-lib build:watch\" \"nodemon -w packages/shared-lib/dist -x 'yarn workspace ui server'\"",
    "client": "yarn && yarn workspace ui client"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "modelfusion": "^0.41.1",
    "ts-node": "^10.9.1",
    "zod": "^3.22.4",
    "zod-to-json-schema": "^3.21.4"
  },
  "devDependencies": {
    "@types/node": "^20.8.3",
    "nodemon": "^3.0.1"
  }
}
